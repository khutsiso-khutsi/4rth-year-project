@* <div class="modal-overlay" id="doctor-modal">
    <div class="modal">
        <div class="modal-head">
            <h2 id="doctor-modal-title">Add Doctor</h2>
            <button class="modal-close" onclick="Controller.closeModal('doctor-modal')">✕</button>
        </div>
        <div class="modal-body">
            <input type="hidden" id="doctor-edit-id" />
            <div class="field">
                <label>Full Name</label>
                <input id="doctor-name" type="text" placeholder="Dr. First Last" />
            </div>
            <div class="field">
                <label>Email</label>
                <input id="doctor-email" type="email" placeholder="doctor@nmbhdl.co.za" />
            </div>
            <div class="field">
                <label>HPCSA Number</label>
                <input id="doctor-hpcsa" type="text" placeholder="HP-0000000" />
            </div>
            <div class="field">
                <label>Status</label>
                <select id="doctor-status">
                    <option value="Active">Active</option>
                    <option value="Inactive">Inactive</option>
                </select>
            </div>
        </div>
        <div class="modal-foot">
            <button class="btn-cancel" onclick="Controller.closeModal('doctor-modal')">Cancel</button>
            <button class="btn-submit teal" onclick="Controller.saveDoctor()">Save Doctor</button>
        </div>
    </div>
</div> *@